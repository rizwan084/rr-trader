# Architecture

apps/web = Next.js frontend. apps/api = Fastify + WebSocket backend. packages/market-data = exchange adapters. packages/signal-engine = analysis engine. packages/shared = contracts and utilities. API modules cover auth, users, subscriptions, access-keys, payments, market-data, exchanges, signals, watchlists, admin and notifications.
