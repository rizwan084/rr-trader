# Security

Secrets are never committed. Exchange keys are server-side, encrypted at rest, masked in UI and least privilege. Withdrawal permissions are not required. Auth and exchange endpoints are rate-limited. Every sensitive mutation is auditable.
