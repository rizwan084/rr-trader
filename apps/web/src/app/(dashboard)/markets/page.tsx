"use client";import Link from "next/link";import {useEffect,useState} from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"";const coins=["BTCUSDT","ETHUSDT","BNBUSDT","SOLUSDT","XRPUSDT","DOGEUSDT","ADAUSDT","AVAXUSDT"];
export default function Page(){const[data,setData]=useState<any[]>([]);useEffect(()=>{Promise.all(coins.map(async s=>{try{return await fetch(API+"/api/v1/markets/"+s+"?market=futures").then(r=>r.json())}catch{return {symbol:s,last:0,change24h:0,volume24h:0}}})).then(setData)},[]);return <main className="shell"><header className="page-head"><div><span className="eyebrow">MARKETS</span><h1>Live Markets</h1><p className="muted">Click any coin to open its full chart and setup.</p></div><Link className="button" href="/scanner">Scan Market</Link></header><section className="table-wrap card"><table className="table"><thead><tr><th>Asset</th><th>Price</th><th>24h</th><th>Volume</th><th></th></tr></thead><tbody>{data.map(x=><tr key={x.symbol}><td><Link className="coin-link" href={"/charts?symbol="+x.symbol}>{x.symbol}</Link></td><td>{x.last||"—"}</td><td>{x.change24h==null?"—":x.change24h+"%"}</td><td>{x.volume24h?Number(x.volume24h).toLocaleString():"—"}</td><td><Link href={"/charts?symbol="+x.symbol}>Chart →</Link></td></tr>)}</tbody></table></section></main>}